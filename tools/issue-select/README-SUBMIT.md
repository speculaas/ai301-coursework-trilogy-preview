Upload contents of this folder (from your installed `~/.claude/skills/issue-select/`).
Do not submit an empty template. After you tune the rubric, re-copy:

    rsync -a ~/.claude/skills/issue-select/ tools/issue-select/
